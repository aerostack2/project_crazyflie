attach: true
root: ./
startup_window: mocap
windows:
  - mocap:
      layout:
      panes:
        - ros2 launch mocap_optitrack mocap.launch.py
            namespace:=optitrack
            config_file:=config/mocap_config.yaml
